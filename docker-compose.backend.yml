services:
  backend:
    build:
      context: ../jcdb
    ports:
      - "3001:3001"
    env_file: ../jcdb/.env
    restart: unless-stopped
    networks:
      - rcs-network

networks:
  rcs-network:
    external: true
